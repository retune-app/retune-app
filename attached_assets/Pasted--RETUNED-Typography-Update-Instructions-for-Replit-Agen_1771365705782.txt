# RETUNED — Typography Update Instructions for Replit Agent

## Overview

Update ALL fonts across retuned.app to the new brand typography system. This applies to every page: `/`, `/science`, `/support`, and any other routes.

**Do NOT change any React Native / in-app fonts. This is landing page only.**

-----

## Brand Typography System

|Role                     |Font              |Weight                    |Size                      |Spacing                         |
|-------------------------|------------------|--------------------------|--------------------------|--------------------------------|
|Nav wordmark “RETUNED”   |Outfit            |500                       |17px                      |letter-spacing: 4.5px, uppercase|
|Nav links & CTA button   |Outfit            |500                       |14px                      |—                               |
|Hero headline (h1)       |Cormorant Garamond|600                       |32px mobile / 48px desktop|letter-spacing: -0.5px          |
|Section headlines (h2)   |Cormorant Garamond|600                       |26px mobile / 36px desktop|letter-spacing: -0.3px          |
|Subsection headlines (h3)|Cormorant Garamond|600                       |20px mobile / 26px desktop|—                               |
|h4                       |Cormorant Garamond|600                       |18px mobile / 20px desktop|—                               |
|Body text (p)            |Outfit            |400                       |16px                      |line-height: 1.6                |
|Buttons & CTAs           |Outfit            |500                       |14px                      |letter-spacing: 0.3px           |
|Pills / tags / badges    |Outfit            |400                       |12px                      |letter-spacing: 0.5px           |
|Table text               |Outfit            |400 (cells), 500 (headers)|inherit                   |—                               |
|Footer                   |Outfit            |400                       |inherit                   |—                               |
|Small text / captions    |Outfit            |400                       |12–13px                   |—                               |

-----

## Step 1: Replace Google Fonts Import

Find the current font import in the HTML `<head>` (likely loads Space Grotesk, Inter, and/or Nunito).

**Remove the old import. Replace with:**

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
```

If fonts are imported via CSS `@import`, replace that instead:

```css
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=Outfit:wght@300;400;500;600;700&display=swap');
```

-----

## Step 2: Global CSS Overrides

Add these base styles. They should override all existing font declarations:

```css
/* ============================================
   RETUNED TYPOGRAPHY SYSTEM — Feb 2026
   Cormorant Garamond (headlines) + Outfit (everything else)
   ============================================ */

body {
  font-family: 'Outfit', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-weight: 400;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

h1, h2, h3, h4, h5, h6 {
  font-family: 'Cormorant Garamond', Georgia, 'Times New Roman', serif;
  font-weight: 600;
  line-height: 1.2;
}

p, li, td, th, label, input, textarea, select, span, a, div {
  font-family: 'Outfit', -apple-system, BlinkMacSystemFont, sans-serif;
}

button, [class*="btn"], [class*="button"], [class*="cta"] {
  font-family: 'Outfit', sans-serif;
  font-weight: 500;
}

code, pre, kbd, samp {
  font-family: 'SF Mono', 'Fira Code', 'Consolas', monospace;
}
```

-----

## Step 3: Find-and-Replace Old Font References

Search the ENTIRE codebase (CSS, JS, HTML, JSX) and replace:

```
'Space Grotesk'  →  'Cormorant Garamond'   (when used for headlines/headings)
'Space Grotesk'  →  'Outfit'               (when used for body/UI)
"Space Grotesk"  →  (same logic as above)
'Inter'          →  'Outfit'
"Inter"          →  "Outfit"
```

After replacing, verify font-weight values:

- Space Grotesk headlines at `700` → change to `600` for Cormorant Garamond
- Inter body at `400` or `500` → keep same for Outfit (same weights work)

If `Nunito` is referenced for landing page elements, replace with `Outfit`. Leave any Nunito references in React Native / app code alone.

-----

## Step 4: Update Nav Wordmark

Find where the “RETUNED” text is styled in the navigation bar. Apply:

```css
.logo-text,
.brand-name,
.nav-brand /* use whatever class actually exists */ {
  font-family: 'Outfit', sans-serif;
  font-size: 17px;
  font-weight: 500;
  letter-spacing: 4.5px;
  text-transform: uppercase;
  color: #1a1a2e;
}
```

**Also remove the gold underline on “TU”** if it exists — the new wordmark is clean with no underline accent. Remove any `<span>` or `border-bottom` styling that creates the underline on specific letters.

-----

## Step 5: Headline Sizes (Responsive)

```css
/* Mobile-first */
h1 { font-size: 32px; line-height: 1.15; letter-spacing: -0.5px; }
h2 { font-size: 26px; line-height: 1.2; letter-spacing: -0.3px; }
h3 { font-size: 20px; line-height: 1.3; }
h4 { font-size: 18px; line-height: 1.3; }

/* Tablet */
@media (min-width: 768px) {
  h1 { font-size: 42px; }
  h2 { font-size: 32px; }
  h3 { font-size: 24px; }
  h4 { font-size: 20px; }
}

/* Desktop */
@media (min-width: 1024px) {
  h1 { font-size: 48px; }
  h2 { font-size: 36px; }
  h3 { font-size: 26px; }
  h4 { font-size: 20px; }
}
```

-----

## Step 6: Section-Specific Treatments

### Hero Section

```css
.hero h1, [class*="hero"] h1 {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-weight: 600;
  color: #1a1a2e;
}

.hero p, [class*="hero"] p {
  font-family: 'Outfit', sans-serif;
  font-weight: 400;
  font-size: 16px;
  line-height: 1.6;
  color: #888;
}
```

### Pills / Tags (e.g. “Personalized Wellness”)

```css
.pill, [class*="pill"], [class*="tag"], [class*="badge"] {
  font-family: 'Outfit', sans-serif;
  font-weight: 400;
  font-size: 12px;
  letter-spacing: 0.5px;
}
```

### CTA Buttons

```css
.cta, [class*="cta"], .btn, [class*="button"] {
  font-family: 'Outfit', sans-serif;
  font-weight: 500;
  font-size: 14px;
  letter-spacing: 0.3px;
}
```

### Feature Cards

```css
[class*="card"] h3, [class*="feature"] h3 {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-weight: 600;
}

[class*="card"] p, [class*="feature"] p {
  font-family: 'Outfit', sans-serif;
  font-weight: 400;
}
```

### Comparison Table (if exists)

```css
table th {
  font-family: 'Outfit', sans-serif;
  font-weight: 500;
}

table td {
  font-family: 'Outfit', sans-serif;
  font-weight: 400;
}
```

### Footer

```css
footer, footer * {
  font-family: 'Outfit', sans-serif;
  font-weight: 400;
}

footer h3, footer h4 {
  font-family: 'Outfit', sans-serif;
  font-weight: 500;
}
```

### Science Page (/science) — Additional

Any scientific references, citations, or data callouts:

```css
.stat-number, [class*="stat"] {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-weight: 600;
}

blockquote {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-weight: 400;
  font-style: italic;
}
```

-----

## Step 7: Pages Checklist

Apply to ALL of these:

- [ ] **`/`** — Main landing page (hero, features, comparison, CTA, footer)
- [ ] **`/science`** — Science page (all sections, stats, citations)
- [ ] **`/support`** — Support page (FAQ, contact, legal)
- [ ] **`/support#privacy`** — Privacy policy section
- [ ] **`/support#terms`** — Terms of service section
- [ ] **Any other routes** that share the layout or have their own styles

If pages share a common CSS file or layout component, changes should propagate automatically. If any page has isolated styles or its own font imports, update those individually.

-----

## Step 8: Verify After Deployment

### Visual Checklist

- [ ] Nav wordmark “RETUNED” → Outfit 500, 17px, 4.5px tracking, uppercase, no gold underline
- [ ] Hero h1 → Cormorant Garamond 600, serif, 32px mobile / 48px desktop
- [ ] Section h2s → Cormorant Garamond 600, serif
- [ ] Subsection h3s → Cormorant Garamond 600, serif
- [ ] All body text → Outfit 400, clean geometric sans
- [ ] CTA buttons → Outfit 500
- [ ] Pills/tags → Outfit 400, 12px
- [ ] Footer → Outfit 400
- [ ] Tables → Outfit

### Technical Checklist

- [ ] Google Fonts loads Cormorant Garamond + Outfit (check DevTools → Network tab → Font)
- [ ] Zero references to `Space Grotesk` remain in any CSS/JS/HTML
- [ ] Zero references to `Inter` remain in any CSS/JS/HTML (landing page files only)
- [ ] No FOUT (flash of unstyled text) on page load — preconnect hints in place
- [ ] All three pages (`/`, `/science`, `/support`) render correctly
- [ ] Mobile Safari renders correctly (test on iPhone)
- [ ] Desktop Chrome renders correctly

-----

## Quick Reference: Brand Colors

```
Navy dark:    #1A1A2E
Navy deep:    #0F1C3F
Gold:         #C9A227
Gold light:   #E5C95C
Text dark:    #1a1a2e
Text body:    #888
Text light:   #aaa
White:        #ffffff
Background:   #faf9f5
```